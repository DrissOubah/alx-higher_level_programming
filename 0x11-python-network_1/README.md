0. What's my status? #0
Write a Python script using urllib to fetch the status of the URL https://alx-intranet.hbtn.io/status.
Display information about the response body, including its type, content, and utf8 content.
Use a with statement.
1. Response header value #0
Write a Python script that takes a URL as an argument, sends a request, and displays the value of the X-Request-Id variable from the response header.
Use urllib and sys.
Utilize a with statement.
2. POST an email #0
Write a Python script that takes a URL and an email as arguments, sends a POST request to the URL with the email as a parameter.
Display the body of the response decoded in utf-8.
Use urllib and sys.
Utilize a with statement.
3. Error code #0
Write a Python script that takes a URL, sends a request, and displays the body of the response.
Handle urllib.error.HTTPError exceptions and print the error code if the status code is greater than or equal to 400.
Use urllib and sys.
Utilize a with statement.
4. What's my status? #1
Write a Python script using the requests library to fetch the status of the URL https://alx-intranet.hbtn.io/status.
Display information about the response body, including its type and content.
5. Response header value #1
Write a Python script that takes a URL as an argument, sends a request, and displays the value of the X-Request-Id variable from the response header.
Use requests and sys.
6. POST an email #1
Write a Python script that takes a URL and an email as arguments, sends a POST request to the URL with the email as a parameter.
Display the body of the response.
Use requests and sys.
7. Error code #1
Write a Python script that takes a URL, sends a request, and displays the body of the response.
Print an error message if the HTTP status code is greater than or equal to 400.
Use requests and sys.
8. Search API
Write a Python script that takes a letter as an argument and sends a POST request to http://0.0.0.0:5000/search_user.
Display user information (id and name) if the JSON response is properly formatted and not empty.
Handle cases where the JSON is invalid or empty.
Use requests and sys.
9. My GitHub!
Write a Python script that takes GitHub credentials (username and personal access token) and uses the GitHub API to display the user's id.
Use Basic Authentication with a personal access token as the password.
Use requests and sys.
