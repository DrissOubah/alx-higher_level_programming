0. Created a constant variable with the value "JavaScript is amazing" and printed it using console.log.

1. Printed three lines: "C is fun," "Python is cool," and "JavaScript is amazing" using console.log.

2. Checked the number of arguments passed to the script and printed a message accordingly: "No argument," "Argument found," or "Arguments found."

3. Printed the first argument passed to the script or "No argument" if none was provided using console.log.

4. Concatenated and printed two arguments in the format " is " using console.log.

5. Checked if the first argument could be converted to an integer and printed either "Not a number" or "My number: <integer>" using console.log.

6. Printed three lines using an array of strings and a loop without using any if/else statements.

7. Printed "C is fun" a specified number of times based on the first argument or "Missing number of occurrences" if no argument was provided.

8. Printed a square of 'X' characters based on the size provided as the first argument or "Missing size" if none was provided.

9. Defined a function named "add" to compute and print the addition of two integers using console.log.

10. Computed and printed the factorial of an integer using a recursive function and console.log.

11. Searched and printed the second biggest integer from the list of arguments using console.log.

12. Updated a script to replace the value 12 with 89 in an object and printed the modified object using console.log.

13. Created a function named "add" in a separate file for adding two integers, which was then imported and used in another script.

