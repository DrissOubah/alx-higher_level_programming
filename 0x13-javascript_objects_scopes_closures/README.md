0. Rectangle #0

An empty class Rectangle is defined in 0-rectangle.js.
An instance of Rectangle is created in 0-main.js, and its properties are logged to the console.
1. Rectangle #1

Class Rectangle is defined in 1-rectangle.js with a constructor that takes width and height as arguments.
Instance attributes width and height are initialized in the constructor.
Instances of Rectangle are created in 1-main.js, and their properties are logged to the console.
2. Rectangle #2

Class Rectangle is defined in 2-rectangle.js with a constructor similar to the previous task.
Additionally, if width or height is equal to 0 or not a positive integer, an empty object is created.
Instances of Rectangle are created in 2-main.js, and their properties are logged to the console.
3. Rectangle #3

Class Rectangle is defined in 3-rectangle.js with a constructor and additional logic to create an empty object if width or height is not a positive integer.
An instance method print() is added to print the rectangle using the character 'X'.
Instances of Rectangle are created in 3-main.js, and their rectangles are printed using the print() method.
4. Rectangle #4

Class Rectangle is defined in 4-rectangle.js with a constructor and instance methods: print(), rotate(), and double().
print() prints the rectangle using 'X', rotate() exchanges width and height, and double() multiplies width and height by 2.
Instances of Rectangle are created in 4-main.js, and their properties are printed before and after calling the methods.
5. Square #0

Class Square is defined in 5-square.js that inherits from Rectangle using the extends keyword.
The constructor of Rectangle is called using super() in the Square constructor.
Instances of Square are created in 5-main.js, and their rectangles are printed before and after calling the double() method.
6. Square #1

Class Square is defined in 6-square.js that inherits from Square and adds an instance method charPrint(c) to print the rectangle using the character c.
If c is undefined, 'X' is used by default.
Instances of Square are created in 6-main.js, and their rectangles are printed using charPrint() with and without a character specified.
7. Occurrences

A function nbOccurences is defined in 7-occurrences.js that returns the number of occurrences of searchElement in the given list.
The function is tested in 7-main.js with different arrays and search elements.
8. Esrever

A function esrever is defined in 8-esrever.js that reverses the elements of an array without using the built-in reverse method.
The function is tested in 8-main.js with different types of arrays.
9. Log me

A function logMe is defined in 9-logme.js that prints the number of arguments already printed and the new argument value.
The function is tested in 9-main.js with different argument values.
10. Number conversion

A function converter is defined in 10-converter.js that returns a function to convert a number from base 10 to another base passed as an argument.
The function is tested in 10-main.js with different bases and numbers.
These tasks cover the creation of classes, inheritance, instance methods, function implementations, and testing.
